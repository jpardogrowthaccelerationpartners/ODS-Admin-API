meta {
  name: ResourceClaimClaimSets - Reset Authorization Strategies Not Found
  type: http
  seq: 36
}

post {
  url: {{API_URL}}/v3/claimSets/{{CreatedClaimSetId}}/resourceClaimActions/40000/resetAuthorizationStrategies
  body: json
  auth: inherit
}

script:post-response {
  test("POST ResetAuthorizationStrategies: Status code is Not Found", function () {
      expect(res.getStatus()).to.equal(404);
  });
  
  test("POST ResetAuthorizationStrategies: type URN is correct", function () {
      const response = res.getBody();
      expect(response.type).to.equal("urn:ed-fi:admin-api:not-found");
  });
}

settings {
  encodeUrl: true
}
