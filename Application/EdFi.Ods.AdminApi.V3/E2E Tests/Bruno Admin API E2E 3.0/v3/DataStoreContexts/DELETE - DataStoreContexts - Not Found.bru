meta {
  name: DataStoreContexts - Not Found_1
  type: http
  seq: 23
}

delete {
  url: {{API_URL}}/v3/dataStoreContexts/{{NotExistDataStoresContextId}}
  body: none
  auth: inherit
}

script:post-response {
  test("DELETE DataStoreContexts NotFound: Status code is Not Found", function () {
      expect(res.getStatus()).to.equal(404);
  });
  
  test("DELETE DataStoreContexts NotFound: Response matches error format", function () {
      const response = res.getBody();
  
      expect(response).to.have.property("title");
  });
  
  test("DELETE DataStoreContexts NotFound: Response title is helpful and accurate", function () {
      const response = res.getBody();
  
      expect(response.title).to.contain("Not found");
      expect(response.title).to.contain("DataStoreContext");
      expect(response.title).to.contain(bru.getVar("NotExistDataStoresContextId"));
  });
  
  test("DELETE DataStoreContexts NotFound: type URN is correct", function () {
      const response = res.getBody();
      expect(response.type).to.equal("urn:ed-fi:admin-api:not-found");
  });
}

settings {
  encodeUrl: true
}
