meta {
  name: Reset Credential - Not Found
  type: http
  seq: 26
}

put {
  url: {{API_URL}}/v3/applications/{{CreatedApplicationId}}/reset-credential
  body: none
  auth: inherit
}

script:post-response {
  test("PUT Reset Credential NotFound: Status code is Not Found", function () {
      expect(res.getStatus()).to.equal(404);
  });
  
  test("PUT Reset Credential NotFound: Response matches error format", function () {
      const response = res.getBody();
      expect(response).to.have.property("title");
  });
  
  test("PUT Reset Credential NotFound: Response title is helpful and accurate", function () {
      const response = res.getBody();
  
      expect(response.title).to.contain("Not found");
      expect(response.title).to.contain("application");
      expect(response.title).to.contain(bru.getVar("CreatedApplicationId"));
  });
  
  test("PUT Reset Credential NotFound: type URN is correct", function () {
      const response = res.getBody();
      expect(response.type).to.equal("urn:ed-fi:admin-api:not-found");
  });
  
}

settings {
  encodeUrl: true
}
