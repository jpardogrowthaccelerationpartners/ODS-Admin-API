meta {
  name: DataStores - Applications associated
  type: http
  seq: 23
}

delete {
  url: {{API_URL}}/v3/dataStores/{{DataStoreId}}
  body: none
  auth: inherit
}

script:post-response {
  test("DELETE DataStore Applications associated: Status code is Bad Request", function () {
      expect(res.getStatus()).to.equal(400);
  });
  
  const response = res.getBody();
  
  test("DELETE DataStore Applications associated: Response matches error format", function () {
      expect(response).to.have.property("title");
      expect(response).to.have.property("errors");
  });
  
  test("DELETE DataStore Applications associated: Response title is helpful and accurate", function () {
      expect(response.title.toLowerCase()).to.contain("validation");
  });
  
  test("DELETE DataStore Applications associated: Response errors include messages by property", function () {
      expect(response.errors.Id[0]).to.contain("Can not be deleted");
      expect(response.errors.Id[0]).to.contain("Applications"); 
  });
  
  test("DELETE DataStore Applications associated: type URN is correct", function () {
      const response = res.getBody();
      expect(response.type).to.equal("urn:ed-fi:admin-api:bad-request:validation");
  });
  
}

settings {
  encodeUrl: true
}
