meta {
  name: DataStoreDerivatives - Invalid Derivative Type
  type: http
  seq: 4
}

post {
  url: {{API_URL}}/v3/dataStoreDerivatives/
  body: json
  auth: inherit
}

body:json {
  {
    "dataStoreId": {{DataStoreId}},
    "derivativeType": "another-derivative-type",
    "connectionString": "{{connectionString}}"
  }
}

script:post-response {
  test("POST DataStore Derivatives Invalid: Status code is Bad Request", function () {
      expect(res.getStatus()).to.equal(400);
  });
  
  const response = res.getBody();
  
  test("POST DataStore Derivatives Invalid: DerivativeType included", function () {
      expect(response.errors).to.have.property('DerivativeType');
  });
  
  test("POST DataStore Derivatives Invalid: DerivativeType value allowed", function () {
       expect(response.errors.DerivativeType[0]).to.contain("The value for the Derivative type is not allowed. The only accepted values are: 'ReadReplica' or 'Snapshot'");
  });
  
  test("POST DataStore Derivatives Invalid: type URN is correct", function () {
      const response = res.getBody();
      expect(response.type).to.equal("urn:ed-fi:admin-api:bad-request:validation");
  });
  
  
}

settings {
  encodeUrl: true
}
