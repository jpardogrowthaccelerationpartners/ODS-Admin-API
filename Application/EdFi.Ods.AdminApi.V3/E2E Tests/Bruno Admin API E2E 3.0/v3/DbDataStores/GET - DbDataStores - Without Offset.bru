meta {
  name: DbDataStores - Without Offset
  type: http
  seq: 11
}

get {
  url: {{API_URL}}/v3/dbDataStores?limit={{limit}}
  body: none
  auth: inherit
}

params:query {
  limit: {{limit}}
}

script:post-response {
  test("GET DbDataStores Without Offset: Status code is OK", function () {
    expect(res.getStatus()).to.equal(200);
  });

  test("GET DbDataStores Without Offset: Response is an array", function () {
    expect(res.getBody()).to.be.an("array");
  });
}

settings {
  encodeUrl: true
}
