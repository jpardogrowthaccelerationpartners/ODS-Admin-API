meta {
  name: DbDataStores - Without Limit and Offset
  type: http
  seq: 9
}

get {
  url: {{API_URL}}/v3/dbDataStores
  body: none
  auth: inherit
}

script:post-response {
  test("GET DbDataStores Without Limit and Offset: Status code is OK", function () {
    expect(res.getStatus()).to.equal(200);
  });

  test("GET DbDataStores Without Limit and Offset: Response is an array", function () {
    expect(res.getBody()).to.be.an("array");
  });
}

settings {
  encodeUrl: true
}
